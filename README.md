# claude-code-crash-course
A crash course for learning Claude Code.

The app is organized into learning modules (one per tool category). Each module pairs
two things: the tools your agent has access to in that area, and a worked example of
what it actually builds with them. Open a module to see both side by side.
